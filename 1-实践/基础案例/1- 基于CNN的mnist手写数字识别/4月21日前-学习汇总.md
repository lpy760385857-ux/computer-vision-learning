
# 一、前言

回顾过去四年的本科学习，是平淡的，是为了完成而学习，很少有为了学习而学习.等考上了研，慢慢变得不同了，终于有机会可以选择学习，看想看的书，思考以后要走的路.

学习是一个枯燥的过程，把握好方向可以过得更快乐.近期学习机器学习的时候，带给我的感触很大，**真切地感受到了什么叫做信息差**.我原以为自己接收到的是蛮可以的信息，但并没有，当我还在用Deepseek的时候，别人已经熟练地用上了国外大模型.还有一点，**孤军奋战学习不利于接收有效信息**.因为二战，我已经脱离群体太久，淡忘了之前和同学一起学习以及视频学习的感觉，导致后面一个劲地去啃那本过于厚重的机器学习理论书.跟老师沟通后，我发现，李航的那本书不适合小白学，里面的内容晦涩难懂.如果要选看书学习，是要打持久战的.**相比之下，看吴恩达老师的视频课，学习效率一下提了上来**，也更容易接受，后续读文献和写汇报的时候，速度会变得更快，有不懂或者卡思路再翻书.

总的来看，学习的大体走向良好，唯一不足的是学习劲头不够，缺乏正反馈，中间通过运动做了调整，大脑思路在慢慢变得清晰，但还是存在划水情况.为了提升后续5周的学习效率，我做了一个**NLP****的学习ddl**.在这之前，先把前段时间学过的东西进行汇总.

# 二、基于CNN的mnist手写数字识别
% *每个标题叙述的是完成案例的过程%
## 1.选择Github收藏量最高的案例(Python)

> [!最好找年份较近的代码]
>   代码是2017年的，时间久远，当时没注意看时间，只想着这个项目逻辑清楚，比较容易读懂，也就没管它放到现在是否跑得通，后来跑的时候问题频出，也注意到了这个问题。虽然跑代码的过程复杂了点，但我认为可以在修改代码的时候顺便熟悉一下代码。
> 

## 2.VS Code、WSL、虚拟环境配置

wsl环境配置忘记让ai总结，当时出现的问题很多，跟Deepseek的聊天框找不到了，但其实也不难，下次电脑硬盘再坏，也需要重新配置，可能报错原因又不一样.故不再总结.

## 3.使用Gemni修改旧代码

我很执着于这份旧项目，懒得找一份新的高质量代码了，索性用大模型修改，修改后的代码是可行的（见图片预测和视频预测）.
### 3.1.旧代码
#### 3.1.1.train.py
```
`import numpy as np`
`import pandas as pd`
`import os`

`def flat_to_one_hot(labels):`
    `num_classes = np.unique(labels).shape[0]`
    `num_labels = labels.shape[0]`
    `index_offset = np.arange(num_labels) * num_classes`
    `labels_one_hot = np.zeros((num_labels,num_classes))`
    `labels_one_hot.flat[index_offset + labels.ravel()] = 1`
    `return labels_one_hot`

`def get_mnist_data(validation_size=2000):`
	`data = pd.read_csv('Data/train.csv')`
	`images = data.iloc[:,1:].values`
	`labels = data[[0]].values.ravel()`
	`# Convert the images from uint8 to double:`
	`images = np.multiply(images,1.0/255.0)`
	`# Convert the labels to one hot encoding:`
	`labels = flat_to_one_hot(labels)`
	`# Split the data into validation and training data:`
	`validation_images = images[:validation_size]`
	`validation_labels = labels[:validation_size]`
	`train_images = images[validation_size:]`
	`train_labels = labels[validation_size:]`
	`# Convert the images from flat to matrix form:`
	`train_images = train_images.reshape(train_images.shape[0],1,28,28)`
	`validation_images = validation_images.reshape(validation_images.shape[0],1,28,28)`
	`# Return the data:`
	`return (train_images,train_labels),(validation_images,validation_labels)`

`from keras.models import Sequential`
`from keras.layers import Dense, Dropout, Activation, Flatten`
`from keras.layers import Convolution2D, MaxPooling2D`

`if __name__ == '__main__':`
    `(X_train,y_train),(X_val,y_val) = get_mnist_data()`
    `# Construct the model:`
    `model = Sequential()`
    `model.add(Convolution2D(nb_filter=32,nb_row=5,nb_col=5,border_mode='same',input_shape=(1,28,28)))`
    `model.add(Activation('relu'))`
    `model.add(MaxPooling2D(pool_size=(2,2),border_mode='same'))`
    `model.add(Convolution2D(nb_filter=64,nb_row=5,nb_col=5,border_mode='same',input_shape=(32,14,14)))`
    `model.add(Activation('relu'))`
    `model.add(MaxPooling2D(pool_size=(2,2),border_mode='same'))`
    `model.add(Flatten())`
    `model.add(Dense(1024))`
    `model.add(Activation('relu'))`
    `model.add(Dropout(0.5))`
    `model.add(Dense(10))`
    `model.add(Activation('softmax'))`
    `model.compile(loss='categorical_crossentropy',optimizer='adam',metrics=['accuracy'])`
    `if os.path.exists('./model_weights.h5'):`
        `model.load_weights('model_weights.h5')`
    `# Train the model:`
    `model.fit(X_train,y_train,batch_size=50,nb_epoch=1,verbose=1,validation_data=(X_val,y_val))`
    `score = model.evaluate(X_val,y_val,verbose=0)`
    `print('Validation score:', score[0])`
    `print('Validation accuracy:', score[1])`
    `# Save the model:`
    `json_string = model.to_json()`
    `open('model_architecture.json','w').write(json_string)`
    `# Save the weights`
    `model.save_weights('model_weights.h5',overwrite=True)`
```
#### 3.1.2.test.py

```
`import numpy as np`
`import pandas as pd`
`from keras.models import model_from_json`

`def get_model():`
    `# Load the model architecture`
    `model = model_from_json(open('model_architecture.json').read())`
    `# Load the model weights`
    `model.load_weights('model_weights.h5')`
    `return model`

`def get_mnist_test_data():`
    `data = pd.read_csv('Data/test.csv')`
    `images = data.values`
    `images = np.multiply(images,1.0/255.0)`
    `images = images.reshape(images.shape[0],1,28,28)`
    `return images`

`if __name__=='__main__':`
    `X_test = get_mnist_test_data()`
    `model = get_model()`
    `y_test = np.argmax(model.predict(X_test,batch_size=50,verbose=1),axis=1)`
    `# Save the predictions:`
    `np.savetxt('predictions.csv',np.c_[range(1,X_test.shape[0]+1),y_test],delimiter=',',header = 'ImageId,Label',comments = '',fmt='%d')`
    `print('Predictions File Generated!')`
```
#### 3.1.3.display_random.py

```
`import numpy as np`
`import matplotlib.pyplot as plt`
`import matplotlib.cm as cm`
`from test import get_model,get_mnist_test_data`
`import time`

`X_test = get_mnist_test_data()`
`model = get_model()`

# `Get 25 random numbers between 0 and 28000`
`random_idx = np.random.random_integers(0,28000,size=25)`

# `Predict for the randomly selected 25 images`
`y_test = np.argmax(model.predict(X_test[random_idx],verbose=0),axis=1)`

# `A function to generate a grid of the 25 images with their corresponding predicted values`
`def generate_grid():`
    `plt.figure()`
    `for i in range(25):`
        `plt.subplot(5,5,i)`
        `plt.axis('off')`
        `plt.imshow(X_test[random_idx[i],0,:,:],cmap=cm.binary)`
        `plt.text(3,5,str(y_test[i]),fontsize=15,bbox={'alpha':0.3,'pad':9})`
    `plt.show()`

# `Display the images with their predicted values at intervals of 5 seconds`
`for i in range(25):`
	`plt.figure()`
	`plt.axis('off')`
	`plt.imshow(X_test[random_idx[i],0,:,:],cmap=cm.binary)`
	`plt.text(3,4,str(y_test[i]),fontsize=40,bbox={'alpha':0.5,'pad':10})`
	`plt.show(block=False)`
	`time.sleep(5)`
	`plt.close()`

# `Display the grid of images and their predicted values`
`generate_grid()`
```

### 3.2.修改的代码
#### 3.2.1.train.py
```
`import numpy as np`
`import pandas as pd`
`import os`
`import tensorflow as tf`
`from tensorflow.keras import layers, models`

`def flat_to_one_hot(labels):`
    `num_classes = np.unique(labels).shape[0]`
    `num_labels = labels.shape[0]`
    `index_offset = np.arange(num_labels) * num_classes`
    `labels_one_hot = np.zeros((num_labels, num_classes))`
    `# 使用 ravel() 确保索引正确`
    `labels_one_hot.flat[index_offset + labels.ravel().astype(int)] = 1`
    `return labels_one_hot`

`def get_mnist_data(validation_size=2000):`
    `# 【新增的自动化魔法】：如果找不到数据集，就自动下载并生成一个！`
    `if not os.path.exists('Data/train.csv'):`
        `print("【自动修复】未检测到 Data/train.csv！")`
        `print("原因：GitHub 限制上传大文件，原作者没有把数据集传上来。")`
        `print("正在自动下载 MNIST 数据集并为您生成 Data/train.csv (可能需要十几秒)...")`
        
        `# 创建 Data 文件夹`
        `os.makedirs('Data', exist_ok=True)`
        
        `# 使用 tf 内置的方法安全下载数据`
        `(x_t, y_t), _ = tf.keras.datasets.mnist.load_data()`
        `x_t_flat = x_t.reshape(x_t.shape[0], -1) # 将 28x28 展平为 784`
        
        `# 拼装成原作者代码需要的 Kaggle CSV 格式：第一列是 label，后面是像素`
        `combined = np.column_stack((y_t, x_t_flat))`
        
        `# 生成 CSV，加上表头兼容 pandas 读取`
        `df = pd.DataFrame(combined)`
        `df.to_csv('Data/train.csv', index=False, header=True)`
        `print("✅ Data/train.csv 自动生成完毕！继续执行...")`

    `print("正在读取 CSV 数据，请稍候...")`
    `data = pd.read_csv('Data/train.csv')`
    
    `# 强制将提取出来的数据转换为 float32 和 int32 类型。彻底避免类型报错。`
    `images = data.iloc[:, 1:].values.astype(np.float32)`
    `labels = data.iloc[:, 0].values.ravel().astype(np.int32)`
    
    `# 归一化 (使用最新的 numpy 写法)`
    `images = images / 255.0`
    
    `# 转换为独热编码`
    `labels = flat_to_one_hot(labels)`
    
    `# 划分数据集`
    `validation_images = images[:validation_size]`
    `validation_labels = labels[:validation_size]`
    `train_images = images[validation_size:]`
    `train_labels = labels[validation_size:]`
    
    `# 适配 TF 2.15 的默认维度 (样本数, 宽, 高, 通道数)`
    `train_images = train_images.reshape(train_images.shape[0], 28, 28, 1)`
    `validation_images = validation_images.reshape(validation_images.shape[0], 28, 28, 1)`
    
    `return (train_images, train_labels), (validation_images, validation_labels)`

`if __name__ == '__main__':`
    `# 1. 获取数据`
    `(X_train, y_train), (X_val, y_val) = get_mnist_data()`
    `print(f"成功加载数据！训练集维度: {X_train.shape}")`
    
    `# 2. 构建模型 (适配 TF 2.15 语法)`
    `model = models.Sequential()`
    
    `model.add(layers.Conv2D(filters=32, kernel_size=(5, 5), padding='same',` 
                            `activation='relu', input_shape=(28, 28, 1)))`
    `model.add(layers.MaxPooling2D(pool_size=(2, 2), padding='same'))`
    
    `model.add(layers.Conv2D(filters=64, kernel_size=(5, 5), padding='same',` 
                            `activation='relu'))`
    `model.add(layers.MaxPooling2D(pool_size=(2, 2), padding='same'))`
    
    `model.add(layers.Flatten())`
    `model.add(layers.Dense(1024, activation='relu'))`
    `model.add(layers.Dropout(0.5))`
    `model.add(layers.Dense(10, activation='softmax'))`
    
    `# 3. 编译模型`
    `model.compile(loss='categorical_crossentropy',` 
                  `optimizer='adam',` 
                  `metrics=['accuracy'])`
    
    `# 4. 加载权重 (如果存在)`
    `if os.path.exists('./model_weights.h5'):`
        `print("发现预训练权重，正在尝试加载...")`
        `try:`
            `model.load_weights('model_weights.h5')`
            `print("权重加载成功！")`
        `except Exception as e:`
            `print(f"警告：无法加载旧权重 (原因: {e})")`
            `print(">>> 没关系！检测到模型形状不兼容，我们将直接从头开始训练新模型！ <<<")`
    
    `# 5. 训练模型`
    `print("开始训练...")`
    `model.fit(X_train, y_train,` 
              `validation_data=(X_val, y_val),` 
              `epochs=10,` 
              `batch_size=64)`
    
    `# 6. 保存权重`
    `model.save_weights('model_weights.h5')`
    `print("训练完成，权重已保存至 model_weights.h5")`
```
#### 3.2.2.test.py
```
import numpy as np
import pandas as pd
import os
import tensorflow as tf
from tensorflow.keras import layers, models

def get_model():
    # 由于原仓库的 model_architecture.json 是旧格式，TF 2.15 无法直接解析。
    # 我们用新语法重新搭建与 train.py 完全一致的网络结构
    model = models.Sequential()
    model.add(layers.Conv2D(filters=32, kernel_size=(5, 5), padding='same', 
                            activation='relu', input_shape=(28, 28, 1)))
    model.add(layers.MaxPooling2D(pool_size=(2, 2), padding='same'))
    
    model.add(layers.Conv2D(filters=64, kernel_size=(5, 5), padding='same', 
                            activation='relu'))
    model.add(layers.MaxPooling2D(pool_size=(2, 2), padding='same'))
    
    model.add(layers.Flatten())
    model.add(layers.Dense(1024, activation='relu'))
    model.add(layers.Dropout(0.5))
    model.add(layers.Dense(10, activation='softmax'))
    
    # 加载你在 train.py 中刚刚训练出来的权重
    if os.path.exists('model_weights.h5'):
        model.load_weights('model_weights.h5')
        print("成功加载 model_weights.h5 权重文件！")
    else:
        print("警告：未找到 model_weights.h5，请确保已经先运行过 train.py")
    return model

def get_mnist_test_data():
    if not os.path.exists('Data/test.csv'):
        raise FileNotFoundError("找不到 Data/test.csv，请检查数据集路径")
        
    print("正在读取测试集 CSV 数据，请稍候...")
    data = pd.read_csv('Data/test.csv')
    
    # 强制转换为 float32 并归一化，避免报错
    images = data.values.astype(np.float32)
    images = images / 255.0
    
    # 适配 TF 2.15 的默认维度 (样本数, 宽, 高, 通道数)
    images = images.reshape(images.shape[0], 28, 28, 1)
    return images

if __name__=='__main__':
    X_test = get_mnist_test_data()
    model = get_model()
    
    print("模型已准备就绪，正在进行批量预测...")
    # 使用训练好的模型对测试集进行预测
    y_test = np.argmax(model.predict(X_test, batch_size=64, verbose=1), axis=1)
    
    # 保存预测结果为 CSV 文件，符合 Kaggle 提交格式
    np.savetxt('predictions.csv', np.c_[range(1, X_test.shape[0]+1), y_test], 
               delimiter=',', header='ImageId,Label', comments='', fmt='%d')
    print('Predictions File Generated! (预测结果已成功保存为 predictions.csv)')
```
#### 3.2.3.display_random.py

```
import numpy as np
import matplotlib
# 告诉 matplotlib 纯后台渲染，不要寻找物理显示器
matplotlib.use('Agg') 
import matplotlib.pyplot as plt
import matplotlib.cm as cm
from test import get_model, get_mnist_test_data

def generate_grid():
    print("正在加载测试集数据和模型...")
    X_test = get_mnist_test_data()
    model = get_model()

    # 获取 25 个随机索引。使用 randint 替代已被弃用的 random_integers
    random_idx = np.random.randint(0, X_test.shape[0], size=25)

    print("正在为随机选取的25张图片进行预测...")
    # 预测随机选取的 25 张图片
    y_test = np.argmax(model.predict(X_test[random_idx], verbose=0), axis=1)

    # 生成一个包含 25 张图片及预测值的网格
    plt.figure(figsize=(10, 10))
    for i in range(25):
        # 新版 matplotlib 的 subplot 索引必须从 1 开始
        plt.subplot(5, 5, i + 1)
        plt.axis('off')
        
        # 适配 TF 2.x 的通道后置。从 [:,0,:,:] 或 [0,:,:] 变成了 [:,:,0]
        # 注意这里 X_test[random_idx[i]] 的形状是 (28, 28, 1)
        plt.imshow(X_test[random_idx[i], :, :, 0], cmap=cm.binary)
        
        # 将模型预测的数字打印在图片上
        plt.text(3, 5, str(y_test[i]), color='red', fontsize=18, 
                 fontweight='bold', bbox={'facecolor': 'white', 'alpha': 0.8, 'pad': 3})
    
    plt.tight_layout()
    
    # 不使用 plt.show() 弹窗，而是保存为当前目录下的 png 文件
    save_path = 'prediction_results.png'
    plt.savefig(save_path, dpi=300, bbox_inches='tight')
    print(f"\n======================================")
    print(f"✅ 预测网格图已成功生成！")
    print(f"📸 请在你的文件夹中查看图片: {save_path}")
    print(f"======================================")

if __name__ == '__main__':
    generate_grid()

```
## 4.矩池云服务器跑通train.py

使用VS Code租矩池云服务器的官方教程：https://matpool.com/supports/doc-vscode-connect-matpool/

> [!NOTE] 不要点右上角那个播放键运行代码
>①因为播放键是在本地的python环境中运行，当然也要选对的本地python环境（指的是已经安装很多第三方库的环境）
②一定要在终端运行python train.py，选对目录，否则不断报错，血泪教训ing

	注意：每次租服务器的时候，都要保存实例，防止文件丢失.
## 5.本地环境预测效果展示

### 5.1.图片预测

先将手写的照片处理为张量形式(CSV)，initiate.py代码：
```
import cv2

import numpy as np

import pandas as pd

import os

  

def process_multi_digit_to_csv(image_path, output_csv):

    """

    将包含多个手写数字的单张图片，切割并转化为 Kaggle 标准的 CSV 格式

    """

    if not os.path.exists(image_path):

        print(f"❌ 找不到图片：{image_path}")

        return

  

    print(f"正在处理图片：{image_path} ...")

    # 1. 读取并二值化

    img = cv2.imread(image_path)

    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

    _, thresh = cv2.threshold(gray, 0, 255, cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU)

    # 2. 寻找轮廓

    contours, _ = cv2.findContours(thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)

    # 3. 提取边界框，并按照 x 坐标从左到右排序（保证提取顺序与书写顺序一致）

    bounding_boxes = [cv2.boundingRect(c) for c in contours]

    bounding_boxes = sorted(bounding_boxes, key=lambda b: b[0])

    all_flattened_digits = []

    # 4. 循环切割每一个数字

    for i, (x, y, w, h) in enumerate(bounding_boxes):

        # 过滤掉太小的噪点

        if w < 5 or h < 5:

            continue

        # 留出一点内边距(Padding)，防止数字紧贴边缘

        pad = 10

        y_start = max(0, y - pad)

        y_end = min(thresh.shape[0], y + h + pad)

        x_start = max(0, x - pad)

        x_end = min(thresh.shape[1], x + w + pad)

        # 裁剪出独立的数字小图

        digit_roi = thresh[y_start:y_end, x_start:x_end]

        # 强制缩放成 28x28

        digit_resized = cv2.resize(digit_roi, (28, 28))

        # 【关键点】：展平为一维数组 (长度 784)。

        # 注意：这里保留 0-255 的整数，除以 255.0 的操作留给 test.py 去做

        digit_flattened = digit_resized.flatten()

        all_flattened_digits.append(digit_flattened)

        print(f"✂️ 成功提取第 {len(all_flattened_digits)} 个数字！")

  

    if not all_flattened_digits:

        print("⚠️ 未检测到任何数字，请检查图片背景是否干净。")

        return

  

    # 5. 将提取出的所有数字转化为 DataFrame 并保存为 CSV

    data_matrix = np.array(all_flattened_digits)

    columns = [f"pixel{i}" for i in range(784)]

    df = pd.DataFrame(data_matrix, columns=columns)

    df.to_csv(output_csv, index=False)

    print("\n=============================================")

    print(f"✅ 处理完成！成功生成: {output_csv}")

    print(f"📊 数据维度: {df.shape} (共 {df.shape[0]} 个数字，每个 784 像素)")

    print("=============================================")

  

if __name__ == '__main__':

    # 假设你上传了一张包含比如 "2026" 四个数字的照片

    target_image = 'image_path2.jpg'

    output_csv_file = 'my_multi_test.csv'

    process_multi_digit_to_csv(target_image, output_csv_file)
```


> [!NOTE] 应提醒Gemni处理手写多个数字的照片
> 初次生成的时候，没写这点，导致代码运行之后CSV文件里只有一行数值

下载训练好的权重文件model_weights.h5，运行test.py，将保存的prediction.csv传入模型，即运行display_random.py.我一共预测两张手写照片，下面是它们的对应效果：

![[image_path.jpg|308]]👉![[prediction_results 1.png|314]]
![[image_path2.jpg|310]]👉![[prediction_results.png|311]]

### 5.2.视频预测

> [!NOTE] 有两点要注意
> 1.没预料USB摄像头坏掉，重新买了一个，导致视频预测搁置几天，效率极低
> 2.直接在wsl环境暂时无法连接摄像头，要在本地Windows环境运行

先关掉VS Code远程连接，然后直接按右上角播放键运行，显示初次预测效果较差，定位框频繁抖动：
             ![[视频预测图.png|409]]
其实也有预测得比较好的情况，没来得及截图.
## 6.c++推理(未完成)

Gemni提供的整体推理步骤：
### 6.1.将模型转换为 ONNX 格式 (在目前的 Python 环境中完成)

您手头现在有的是 `modern_mnist_model.h5`。我们需要用工具把它转成 `.onnx`。

1. 在您的 `cv_env` 虚拟环境中，安装转换工具：
    
    ```
    pip install tf2onnx onnx
    
    ```
    
2. 运行转换命令，将您的 Keras 模型导出为 ONNX：
    
    ```
    python -m tf2onnx.convert --keras modern_mnist_model.h5 --output mnist.onnx
    ```
    
    _运行成功后，您会得到一个 `mnist.onnx` 文件。以后在 C++ 中，我们只需要这个文件，彻底和 TensorFlow 说再见。_
    

### 6.2.搭建 C++ 环境的opencv


> [!NOTE] 基于VS Code
> 我是根据DS提供的步骤一步步调试完成的，整个过程较为繁杂，总结如下：

#### 6.2.1.在本地Windows 配置C++

	本地VS Code配置失败，时间成本较高，新手暂时不推荐...
	目前本地转向Visual Studio是最好的选择，操作简单.

#### 6.2.2.在WSL 配置C++(VS CODE)

    最初尝试在 Windows 原生环境下使用 MinGW-w64 编译器，配合官方预编译的 OpenCV 库（MSVC 版本），遇到了严重的 **ABI 不兼容问题**——MinGW 链接器无法识别 MSVC 格式的 `.lib` 文件，导致编译失败。尽管可以自行从源码编译 OpenCV 以匹配 MinGW，但这需要耗费额外的时间和精力.

##### Ⅰ：安装基础开发工具
首先，确保你的 Windows 已安装 WSL 2 并运行 Ubuntu（或其他发行版）。以下以 Ubuntu 22.04 LTS 为例。
1. 更新系统包列表
```
bash

sudo apt update
sudo apt upgrade -y
```
2. 安装 C++ 编译器与构建工具链
```
bash

sudo apt install build-essential cmake gdb -y
```

- `build-essential`：包含 GCC、g++、make 等核心编译工具。
    
- `cmake`：跨平台构建系统生成器。
    
- `gdb`：调试器。
    
3. 安装 OpenCV 开发库

```
bash

sudo apt install libopencv-dev -y
```
该命令会安装 OpenCV 的核心库、头文件以及所有必要的依赖（如 JPEG、PNG、GTK 等）。安装后，头文件位于 `/usr/include/opencv4`，库文件位于 `/usr/lib/x86_64-linux-gnu`。
4. （可选）安装 ONNX Runtime
若需要 AI 模型推理，可手动下载 Linux 版 ONNX Runtime 预编译包：

```
bash

cd ~
wget https://github.com/microsoft/onnxruntime/releases/download/v1.24.4/onnxruntime-linux-x64-1.24.4.tgz
sudo tar -xzvf onnxruntime-linux-x64-1.24.4.tgz -C /usr/local
```

安装后，库与头文件位于 `/usr/local/onnxruntime-linux-x64-1.24.4/`。

---
##### Ⅱ：创建并构建你的 C++ 项目
 1. 项目目录结构
建议在 Windows 文件系统下创建项目（方便使用 VS Code 编辑），例如 `D:\opencv_test`。在 WSL 中，该路径对应 `/mnt/d/opencv_test`。

%% text

/mnt/d/opencv_test/
├── CMakeLists.txt      # CMake 配置文件
├── test.cpp            # 主程序源文件
├── build/              # 编译输出目录（自动生成）
└── test.jpg            # 测试图片（可选） %%

2. 编写 `CMakeLists.txt`
CMake 负责自动查找库并生成编译规则。以下是一个同时支持 OpenCV 和 ONNX Runtime 的示例：

```
cmake

cmake_minimum_required(VERSION 3.10)
project(OpenCV_ONNX_Test)
set(CMAKE_CXX_STANDARD 17)
# 查找 OpenCV（通过 apt 安装的库 CMake 会自动定位）
find_package(OpenCV REQUIRED)
message(STATUS "OpenCV found: ${OpenCV_DIR}")
# 手动指定 ONNX Runtime 路径
set(ONNXRUNTIME_ROOT "/usr/local/onnxruntime-linux-x64-1.24.4")
include_directories(${ONNXRUNTIME_ROOT}/include)
link_directories(${ONNXRUNTIME_ROOT}/lib)
# 添加可执行文件
add_executable(test test.cpp)
# 链接库
target_link_libraries(test ${OpenCV_LIBS} onnxruntime)

```
 3. 编写测试程序 `test.cpp`

```
cpp

#include <opencv2/opencv.hpp>
#include <iostream>
int main() {
    cv::Mat image = cv::imread("test.jpg");
    if (image.empty()) {
        std::cout << "Could not read image!" << std::endl;
        return -1;
    }
    cv::imshow("Display", image);
    cv::waitKey(0);
    std::cout << "OpenCV works in WSL!" << std::endl;
    return 0;
}
```

 4. 编译项目

在 WSL 终端中进入项目目录，执行以下命令：

bash

cd /mnt/d/opencv_test
mkdir -p build && cd build
cmake ..
make -j4

- `cmake ..`：读取上级目录的 `CMakeLists.txt`，生成 Makefile。仅在首次或修改 CMakeLists.txt 后需要执行。
    
- `make -j4`：调用 GCC 编译源码，生成可执行文件 `test`。
    

---
##### Ⅲ：解决图形界面显示问题

WSL 本身是命令行环境，无法直接显示 OpenCV 的 `imshow` 窗口。需要借助 **X Server** 将图形输出转发到 Windows 桌面。

 1. 在 Windows 上安装 VcXsrv

- 下载地址：[https://sourceforge.net/projects/vcxsrv/](https://sourceforge.net/projects/vcxsrv/)
    
- 安装后运行 **XLaunch**，选择 "Multiple windows"，Display number 保持 `-1`，一路下一步完成启动。VcXsrv 会在系统托盘常驻。
    

 2. 配置 WSL 的 DISPLAY 变量

在 WSL 终端中执行：

```
bash

echo 'export DISPLAY=$(cat /etc/resolv.conf | grep nameserver | awk '\''{print $2}'\''):0' >> ~/.bashrc
source ~/.bashrc
```

这会将 WSL 的图形输出重定向到 Windows 宿主机的 IP 地址，确保每次打开终端时自动生效。

3. 运行程序

```
bash

cd /mnt/d/opencv_test/build
./test

```
此时应能弹出显示图片的窗口。如果图片不存在，程序会提示 `Could not read image!`，请事先放一张 `test.jpg` 到 `build` 目录或指定绝对路径。

---
#####  Ⅳ：日常使用命令总结

以后每次修改代码后，只需执行三个命令即可完成编译与运行：

```
bash

cd /mnt/d/opencv_test/build   # 进入构建目录
make -j4                      # 重新编译（仅变更的源文件）
./test                        # 运行程序
```

若修改了 `CMakeLists.txt`（如新增源文件或库），则需先运行一次 `cmake ..` 更新构建规则。

---

##### 💡 关键要点与常见问题

| 要点                        | 说明                                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------------- |
| **库的来源**                  | 通过 `apt` 安装的 `libopencv-dev` 与系统 GCC 完全兼容，无需担心 ABI 问题。                                                  |
| **路径映射**                  | WSL 中访问 Windows 文件使用 `/mnt/盘符/路径` 格式，注意大小写敏感。                                                           |
| **权限问题**                  | 若提示 `Permission denied`，给文件添加执行权限：`chmod +x test`。                                                      |
| **运行时找不到 ONNX Runtime 库** | 添加 `export LD_LIBRARY_PATH=/usr/local/onnxruntime-linux-x64-1.24.4/lib:$LD_LIBRARY_PATH` 到 `~/.bashrc`。 |
| **编译时找不到头文件**             | 检查 `include_directories` 路径是否正确，或库是否已正确安装。                                                              |

---

##### 🧩 其他建议

- **使用版本控制**：在项目根目录执行 `git init`，并创建 `.gitignore` 忽略 `build/` 目录。
    
- **多文件组织**：可将 `.cpp` 放入 `src/` 文件夹，头文件放入 `include/`，并在 `CMakeLists.txt` 中相应调整路径。
    
- **调试配置**：在 VS Code 中安装 `WSL` 扩展和 `CMake Tools` 扩展，即可直接在 Windows 的 VS Code 界面下对 WSL 中的代码进行断点调试。
    


---

> [!NOTE] 配置C++总结
> Deepseek总结的有点啰嗦，其实解释起来，大概是这样的：
> 1.如果需要视频预测，选择本地Visual Studio，VS Code不适合小白
> 2.其余不需要调用本地设备的代码，比如图片预测，用wsl就够


---

### 6.3.用 C++ 编写预测代码 (替换掉 python 脚本)

这一步是将您在 Python 里写的逻辑“直译”为 C++ 代码。以下是 C++ 代码的核心逻辑骨架（供您参考和学习）：

```
#include <iostream>
#include <vector>
#include <opencv2/opencv.hpp>
#include <onnxruntime_cxx_api.h>

int main() {
    // 1. 初始化 ORT 环境和加载模型
    Ort::Env env(ORT_LOGGING_LEVEL_WARNING, "MNIST_Inference");
    Ort::SessionOptions session_options;
    // 加载我们第一步转出来的 onnx 模型
    Ort::Session session(env, "mnist.onnx", session_options);

    // 2. 打开 OpenCV 摄像头 (对应 Python: cap = cv2.VideoCapture(0))
    cv::VideoCapture cap(0);
    if (!cap.isOpened()) return -1;

    cv::Mat frame;
    while (cap.read(frame)) {
        // --- 图像预处理部分 (必须和 Python 保持 100% 一致) ---
        cv::Mat gray, blurred, thresh;
        cv::cvtColor(frame, gray, cv::COLOR_BGR2GRAY);
        cv::GaussianBlur(gray, blurred, cv::Size(5, 5), 0);
        cv::adaptiveThreshold(blurred, thresh, 255, cv::ADAPTIVE_THRESH_GAUSSIAN_C, cv::THRESH_BINARY_INV, 15, 5);
        
        // 假设我们这里只取画面中间的一个 28x28 ROI 区域进行演示
        cv::Mat roi = thresh(cv::Rect(frame.cols/2 - 14, frame.rows/2 - 14, 28, 28));
        
        // 归一化 (类似 Python: roi.astype('float32') / 255.0)
        cv::Mat float_roi;
        roi.convertTo(float_roi, CV_32FC1, 1.0f / 255.0f);

        // 3. 将 OpenCV 的 Mat 数据塞入 ORT Tensor 中
        std::vector<float> input_tensor_values(float_roi.begin<float>(), float_roi.end<float>());
        std::vector<int64_t> input_shape = {1, 28, 28, 1}; // NHWC format matching your TF model
        
        auto memory_info = Ort::MemoryInfo::CreateCpu(OrtDeviceAllocator, OrtMemTypeCPU);
        Ort::Value input_tensor = Ort::Value::CreateTensor<float>(memory_info, input_tensor_values.data(), input_tensor_values.size(), input_shape.data(), input_shape.size());

        // 4. 执行推理 (对应 Python: model.predict)
        const char* input_names[] = {"conv2d_input"}; // 请根据实际 ONNX 模型输入节点名称修改
        const char* output_names[] = {"dense_1"};     // 请根据实际 ONNX 模型输出节点名称修改
        
        auto output_tensors = session.Run(Ort::RunOptions{nullptr}, input_names, &input_tensor, 1, output_names, 1);

        // 5. 解析输出结果 (寻找最大概率的索引，对应 Python: np.argmax)
        float* floatarr = output_tensors.front().GetTensorMutableData<float>();
        int predicted_digit = std::distance(floatarr, std::max_element(floatarr, floatarr + 10));

        // 6. 在画面上画框并显示 (对应 Python: cv2.putText, cv2.imshow)
        cv::putText(frame, std::to_string(predicted_digit), cv::Point(50, 50), cv::FONT_HERSHEY_SIMPLEX, 1.5, cv::Scalar(0, 0, 255), 2);
        cv::imshow("C++ ORT Inference", frame);

        if (cv::waitKey(1) == 'q') break;
    }
    return 0;
}

```
==未完待续，后续需要再来搞==
## 7.原理详解-CNN

### 7.1.项目介绍

- 训练数据集来自Kaggle的mnist数据集，每个数字20个，用于模型训练。模型架构与权重分别保存在 `model_architecture.json` 和 `model_weights.h5` 文件中。请注意，这些权重仅与 TensorFlow 后端兼容。
- 运行 `train.py` 即可训练模型。`test.py` 文件会生成一个名为 `predictions.csv` 的文件，其中包含测试集图像的预测标签，该文件可用于在 Kaggle 上提交结果。`display_random.py` 则会从测试集中随机显示 25 张图像及其对应的预测标签。

  ![[Pasted image 20260421144534.png]]
### 7.2.图解训练

- 吴恩达老师说：“可以使用别人的架构用于自己的想法应用。”在这里，我们可以应用吴恩达老师讲卷积神经网络的思路拆解项目.
- 拆解前，先解释作者提供的架构图和模型图.
![[公式.png]]
![[Pasted image 20260421152958.png|555]]

图2是图1的补充，包括卷积层、池化层、全连接层。

![[model.png|529]]

考虑到原模型图为纯文字，因此我重新绘制一份更通俗易懂的模型图。由于加速器卡顿，暂时用不了Gemni，先画一份草图。

![](file:///C:/Users/ADMINI~1/AppData/Local/Temp/msohtmlclip1/01/clip_image002.jpg)

![](file:///C:/Users/ADMINI~1/AppData/Local/Temp/msohtmlclip1/01/clip_image004.jpg)


       由于时间问题，具体的手写数字图片训练过程不详细写了，重点是要通过例子理解卷积神经网络是什么、能梳理模型结构即可。

# 三、NLP学习ddl

再学习1天看具体的进度怎样再写.

# 四、利用Obsidian构建个人知识库

视频学习链接：bilibili.com/video/BV1fZCyBYEuT/?spm_id_from

# 五、总结

将具体知识学习过程归纳为：输入→总结→输出（这是一个闭环）
输出这一环很重要，可以通过尝试复现论文并写报告输出.